import React from 'react'
import { Key } from './Key'

class Piano extends React.Component {

    //Key keys = ['do', 'ray', 'mi', 'fa', 'so', 'la', 'ti', 'do']

    render () {
        return (
            <div className='key'>
                <p>keys go here</p>
            </div>
        )
    }

}

export { Piano }